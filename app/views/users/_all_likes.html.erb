<%= turbo_stream_from "comments" %>
<%= turbo_stream_from "reposts" %>
<%= turbo_stream_from "likes" %>

<%= render "users/page_links" %>

<% likes.each do |like| %>
  <% original = like.tweet.original if like.tweet.repost? || like.tweet.quote_tweet? %>
  <%= render "tweets/single_tweet", t: like.tweet, original: original, view: :likes %>
<% end %>
