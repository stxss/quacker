<%= tag.div id: dom_id(post, :likes), class: liked_class(post) do %>
  <%= button_to post_like_path(post), class: "flex gap-2", method: :patch do %>
    <%= like_btn_icon(post) %>
    <%= tag.div id: dom_id(post, :likes_count) do %>
      <%= post.likes_count %>
    <% end %>
  <% end %>
<% end %>
