<%= button_to "Cancel", follow_path(id: current_user.active_follows.find_by(followed_id: user.id, is_request: true)), method: :delete, form: { data: {turbo_confirm: "This will cancel your pending request, and @#{user.username} will no longer see it.", header: "Discard follow request?", confirm: "Discard", cancel: "Cancel"}}, params: {follow: {followed_id: user.id, is_request: user.account.private_visibility }}, remote: true %>
