<%= render "tweets/creator_info", t: t %>
<div class="tweet-info">

  <%= render "tweets/tweet_body", t: t %>

  <% if original.nil? || !original.deleted_at.nil? %>
    <%= render "tweets/deleted_tweet", t: original, view: view %>
  <% elsif current_user.account.has_blocked?(original.author) || original.author.account.has_blocked?(current_user) %>
      <%= render "tweets/unavailable_tweet" %>
  <% else %>
    <%= turbo_frame_tag "quoted_tweet_#{original.id}", class: "" do %>
      <div class="tweet">
        <%= render "tweets/creator_info", t: original %>
        <%= render "tweets/tweet_body", t: original %>
      </div>
    <% end %>
  <% end %>

  <div class="activity-box">
    <%= render "comments/comments", t: t %>

    <span class="retweets">
      <%= render "retweets/retweets", t: t %>
    </span>

    <span class="likes">
      <%= render "tweets/likes", t: t %>
    </span>
    </span>

  </div>

  <% if view.in?([:single_post, :timeline]) %>
    <div class="nested-comments">
      <%= render "comments/show_comments", t: t, view: view %>
    </div>
  <% end %>
</div>
