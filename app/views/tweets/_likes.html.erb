<%= tag.div id: dom_id(t, :likes), class: "flex gap-2 #{liked_class(t)}" do %>
    <%= button_to like_btn_icon(t), tweet_like_path(t), method: :patch %>
    <%= tag.div id: dom_id(t, :likes_count) do %>
      <%= t.likes_count %>
    <% end %>
<% end %>
