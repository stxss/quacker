<% @tweets.each do |t| %>
  <% if t.comment? && t.new_tweet?(@tweets) && session[:new_comment] == 1  %>
    <%= render "tweets/comment_tweet_info", t: t %>
  <% else %>
    <%= render "tweets/single_tweet", t: t %>
  <% end %>
<% end %>
