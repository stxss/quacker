<%= button_to "xrt", retweet_path(username: current_user.username, id: current_user.created_tweets.find_by(retweet_id: t.id)), params: { retweet: { retweet_id: t.id } }, method: :delete, remote: true, data: { turbo_frame: "retweet-#{t.id}" } %>
