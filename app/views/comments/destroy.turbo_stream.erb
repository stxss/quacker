<%= turbo_stream.replace_all("#tweet_#{@comment.id}.quoted ", partial: "tweets/deleted_tweet", locals: {view: :single_post, t: @comment}) %>
<%= turbo_stream.remove_all(".reposts repost_#{@comment.id}") %>
<%= turbo_stream.remove("tweet_#{@comment.id}")%>
