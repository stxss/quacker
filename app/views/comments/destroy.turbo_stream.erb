<%= turbo_stream.replace_all("#post_#{@comment.id}.quoted ", partial: "posts/deleted_post", locals: {view: :single_post, post: @comment}) %>
<%= turbo_stream.remove_all(".reposts repost_#{@comment.id}") %>
<%= turbo_stream.remove("post_#{@comment.id}")%>
