<%= turbo_stream.remove("post_#{@repost.id}") %>
<%= turbo_stream.replace_all("#repost_#{@repost.original.id}", partial: "reposts/reposts", locals: {post: @repost.original, user: current_user}) %>
<%= render_flash_message %>
