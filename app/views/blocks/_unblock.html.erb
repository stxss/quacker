<%= turbo_frame_tag "unblock-#{user.id}" do %>
  <%= button_to "Unblock @#{user.username}", block_path(id: user.id), method: :delete, form: { data: {turbo_confirm: "They will be able to follow you and view your posts.", header: "Unblock @#{user.username}?", confirm: "Unblock", cancel: "Cancel" }}, remote: true %>
<% end %>
